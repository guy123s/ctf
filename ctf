#!/bin/bash
# Unified CTF Testing Suite

cd "$(dirname "$0")"

# Setup venv if needed
if [ ! -d ".venv" ]; then
    python3 -m venv .venv
    source .venv/bin/activate
    pip install -q rich
else
    source .venv/bin/activate
fi

python3 ctf.py "$@"
