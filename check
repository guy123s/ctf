#!/bin/bash
# Self-check test runner - delegates to unified CTF tool

cd "$(dirname "$0")"

if [ ! -d ".venv" ]; then
    python3 -m venv .venv
    source .venv/bin/activate
    pip install -q rich
else
    source .venv/bin/activate
fi

python3 ctf.py check "$@"
